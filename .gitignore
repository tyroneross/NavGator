# Dependencies
node_modules/

# Build output
# NOTE: dist/ is committed per `feedback_plugin_dist_commit.md` — marketplace
# installs clone the repo without building, so .mcp.json paths to dist/ break
# unless dist/ ships in the repo.
# dist/

# Architecture data (generated per-project)
.navgator/architecture/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Test coverage
coverage/

# Environment
.env
.env.local
.env.*.local
web/node_modules/
web/.next/
web/runtime/
web/server.js
web/server.cjs

# Bookmark snapshot data (.bookmark/ is where the plugin actually writes;
# the earlier .claude/bookmarks/ pattern was a typo that never matched)
.bookmark/
.claude/bookmarks/

# Agent-tooling state (never tracked — plugins store under .<toolname>/)
.build-loop/
.rally/
.codex/
.claude-code-debugger/

# Generated .navgator state inside test fixtures
__tests__/fixtures/**/.navgator/

# >>> NavGator safety guard (auto-managed)
# These files are regenerated from .env on every NavGator scan and contain
# parsed hostnames/endpoints from your live environment. Credentials are
# stripped by NavGator, but hostnames/project refs still leak infra identity.
# Safe to delete this block if you want the files tracked — NavGator will
# not re-add it while the markers below are missing.
.navgator/architecture/components/COMP_config_*.json
.navgator/architecture/components.full.jsonl
.navgator/architecture/connections.full.jsonl
.navgator/architecture/NAVSUMMARY.md
.navgator/architecture/NAVSUMMARY_FULL.md
.navgator/architecture/SUMMARY.md
.navgator/architecture/SUMMARY_FULL.md
.navgator/dirty.json
.navgator/dirty.d/
.navgator/dirty.lock*
.navgator/scan.lock*
.navgator/architecture/freshness.json.tmp*
# <<< NavGator safety guard
