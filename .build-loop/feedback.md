2026-04-12 | NavGator install + global lessons shipped in one pass | Loading plugin-dev skills (plugin-structure, plugin-settings, mcp-integration) during Phase 1 correctly flagged the wrapped-vs-unwrapped .mcp.json ambiguity. Subagent delivered 750 LOC across T6 with 18 new tests passing + zero regressions on 268 existing. Memory bridge (feedback_build_loop_plugin_dev.md) captured so future plugin work auto-loads these skills.

2026-08-03 | Content guard silently dropped two large plan writes | The Write tool reported success but the file never landed, for content containing a literal destructive shell string (`;rm -rf ~`) used as a URL-rejection test case. Small files and the same file without that string persisted. Diagnosis took ~15 min of wrong hypotheses (hook sweep, plan-path filter) because the failure mode is a SILENT success. Next time: after any Write of a file that will carry adversarial test payloads, verify with `ls`/`wc -l` before moving on, and construct destructive payloads by runtime concatenation rather than literal text. Briefed implementers on this and C7 hit the same class (escape sequences converted to raw control bytes) and worked around it.

2026-08-03 | Review found two HIGH data-loss defects that 680 green tests did not | Both were found by MUTATION PROBES, not by reading: registry writes lost 4 of 6 registrations at concurrency 4, and the trace change deleted whole paths. Neither had an oracle. The lesson that generalizes: a new option that changes traversal semantics needs a differential test against the old mode across SEVERAL graph shapes, not one fixture — the first fix passed its own single-fixture test while leaving the defect class open, because the test encoded a contract the code could not hold. Dispatching a fix-critique with execution ability after the fix pass caught it; the earlier critique without Bash could only trace by hand.

2026-08-03 | An "atomic write" fix reintroduced the exact failure it claimed to remove | I replaced a plain writeFile with write-temp-then-rename, but used `pid + Date.now()` as the temp suffix — not unique per call. Under 8 concurrent writers that published unparseable JSON in 123 of 400 rounds, worse than the original. Two lessons: (a) "atomic" is a property of the WHOLE pattern, not the rename alone — the temp name is part of the contract; (b) I shipped it with a passing test, because the test was single-writer. A concurrency fix needs a concurrency oracle, and I only wrote one after an auditor measured the failure. Mutation-checked the new test afterwards: 159 rejections against the old code, 0 against the new.

2026-08-03 | Give verification agents Bash, or they can only trace | The first fix-critique had no execution tool and hand-traced a counterexample it could not run; I had to measure it myself to confirm. The follow-up auditor with Bash ran a 47790-case randomized differential and found four defects the read-only pass missed, including the one above. For any "is this fix real" question, dispatch with execution — a traced hypothesis and a measured result are different evidence classes.

2026-08-03 | Two parallel implementers each returned "complete" and left a dead capability between them | C4 built the build-loop-memory mirror and was correctly told not to wire call sites (another chunk owned src/projects.ts); C3 wired the capture path but its brief predated the mirror's existence. Both were individually right; the seam was unowned, and mirrorProjectMemory would have shipped tested, documented, and never called. MECE file ownership prevents write collisions, not activation gaps — activation is a call-graph property that spans owned-file boundaries by definition. Next time: when dispatching a producer and its consumer in the same or adjacent batches, name the wiring line's owner AT DISPATCH TIME (the way the auditor ladder names who owes the audit), and grep every new export for a non-test, non-defining-file caller before declaring done.

2026-08-03 | I dispatched security-reviewer without Bash and got static analysis when I asked for measured exploitation | The brief said "You have Bash. USE IT — run the code, don't just read it," but the agent's tool list is Read/Grep/Glob. It handled this well (said so plainly, separated verified-by-reading from could-not-verify rather than dressing inference up as evidence), but 3 of its 8 findings stayed unverified and I had to measure them myself. Check the agent definition's tools BEFORE writing a brief that depends on execution — this repo's own feedback log already records "give verification agents Bash, or they can only trace," and I re-learned it from the other side.

2026-08-03 | Running the real binary found two defects that 906 green tests and a passing review did not | `node dist/cli/index.js doctor` on the actual registry reported "≈3222.9 new entries/day" over a 0.1-day window and flagged a clean 2-entry registry as needing attention; `npm run verify:release` on the actual host CLI failed 9!==6 on skills the HOST generates. Both classes are invisible to unit tests because unit tests encode the contract you imagined, not the environment you ship into. Budget a live end-to-end invocation against real data before declaring any CLI feature done.
